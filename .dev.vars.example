# 本地调试用的机密变量。复制为 .dev.vars 后填入真实值（.dev.vars 不要提交）。
# 线上部署时这些值用 `wrangler secret put <名字>` 设置，不走本文件。

BOT_TOKEN=123456789:AAExampleBotTokenReplaceMe
AI_API_KEY=sk-your-deepseek-or-openai-compatible-key
WEBHOOK_SECRET=换成一段足够长的随机字符串
